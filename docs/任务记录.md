# 任务记录

## 2026-09-09

### 已完成

- **库存查询字段与单证**：本票品质展示铜/银/金/砷/铅/镉/氟/汞八项；列表删除类型列；成品原产国为 —；已出库堆位为 —；成品详情仅出库报关单与品质证书
- **出库台账流向企业**：重量台账出库流水记录流向企业；出库列表与已出库演示票 FL-20260801 / CK-20260801-008 对齐
- **货物类型规则**：原料为达标矿/报备矿；成品一律为混成品（库存、物料档案、本票详情）
- **盘点月度在库导出**：盘点管理可按月份导出各委托方在库湿重/干重、堆位、货物类型、提单号、报关单号
- **出库消费使用单位与时间筛选**：出库列表/新建单增加消费使用单位（OCR 可回填）；支持按起止日期查询
- **投料出库详情**：列表增加出库报关单号；物料、船名/航次、提单号、来源国家改在详情展示
- **平面图拖拽与堆位配色**：平移图形只改位置不改宽高；全部堆位背景固定白色；生产线/功能区名称随块大小缩放并换行（窄条竖排）
- **首页平面图加载态**：驾驶舱平面图在接口返回前显示工业风扫描加载，不再先画默认图再替换为已保存布局；加载动画至少展示 2 秒
- **驾驶舱预警滚动**：底栏预警与作业链路一致，无缝循环向上滚动，悬停暂停
- **仓库使用分布**：驾驶舱底栏去掉饼图，仅保留各仓库占用进度条，标题改为「仓库使用分布」
- **驾驶舱平面图配色**：仓库、生产区、堆位使用平面图设置中的填色，不再整图套深色 HUD 底
- **平面图门置顶**：门绘制在堆位等图形之上，始终处于最高层级
- **平面图默认填色**：仓库 `#f8e4c5`、门 `#9ebce9`、堆位白色
- **堆位矿物分色**：南国铜业绿、五矿有色黄、广西金川蓝；达标矿浅色、报备矿深色；混成品灰色

## 2026-09-08

### 已完成

- **平面图缩放条去掉「适应」并移到右上角**：画板缩放条仅保留缩小 / 百分比 / 放大，固定在画板右上角
- **系统参数精简**：移除「盘点差异自动生成」「操作日志保留策略」；操作日志固定永久留存
- **库内移库交互**：移库先选原堆位再选矿批次；批量移库拆成两条独立记录不合并；增加船名、柜数；移库时间为起止到时（不含分）；列表移除操作人与详情
- **入库列表字段与状态**：增加时间范围、货运方式、矿种筛选；去掉片区标识；状态改为暂存码头/待收货/已入库/混成品；单据增加矿种、五项有害元素、装运方式（集装箱/散货）、集装箱柜数
- **保税账册衔接与票矿**：账册增加货值、报关单号；保存出入库票矿；点击编号查看并可导出 Excel。录入新账须填旧账预警日期，旧账改为只出不进；超过预警日后未出库票的入库数据转入新账
- **往来主体去掉信用代码**：列表、搜索、弹窗不再维护统一社会信用代码
- **流向企业自动建档**：往来主体中的流向企业根据出库报关单「流向企业」字段自动识别写入，无需手工新建
- **矿源备案票货与导出**：列表备案编号可点击查看该备案下全部票货；备案列表与票货均可导出 Excel
- **物料档案导出 Excel**：物料档案列表支持导出编码、名称、货物类型、类型、矿源
- **全站账号注册**：系统设置「海关专用账号」改为「账号管理」；新建账号弹窗注册管理员/仓管员/关务员；列表去掉所属关区
- **码头仓库与超期预警**：仓库档案增加码头仓库（堆位如 码头#A1）；货品存放超过系统参数「码头仓库预警」天数（默认 7 天）触发预警
- **角色权限调整**：关务员（海关专用）菜单默认全开；移除生产员角色（生产模块由全开角色或仓管作业覆盖）
- **平面图仓库标号外置**：仓库编号以 4#、5# 等形式标在仓库矩形**下方**居中；贴底时改标上方；仓内堆位名称去掉仓号前缀（如 5#小A → 小A）
- **画板网格密度随平面图保存**：系统设置 · 平面图设置的行×列写入 `gridRows`/`gridCols`，保存后再次打开仍为上次密度
- **网格吸附按行列格线**：吸附改按画板整数格线（四边对齐），避免改密度后面条与格子错开
- **堆位网格取自平面图**：不再在堆位档案编辑行列；按平面图中该堆位所占画板格计算；入库预约必须选择堆位并圈选存放位置
- **入库预约堆位搜索**：预定堆位移到右侧存放位置上方，支持编码/仓名关键字搜索
- **物料档案去掉原产国与附件**：原产国不再维护；附件改到矿源备案列表/弹窗上传与预览
- **堆位档案字段收敛**：堆位管理列表/弹窗移除报关单号、生产批次、占用率、查验状态；保留编码、仓库、片区标识、网格、库容、状态
- **堆位管理移除导出**：堆位页工具栏去掉「导出」按钮
- **仓库面积字段**：仓库管理列表增加「面积(㎡)」列；档案弹窗可编辑；仅档案展示，不参与库容占用计算；编辑结果本地持久化
- **驾驶舱仓库堆位示意图**：由五张仓库卡片改为整园拼接平面图，展示 1/2/4/5/6 仓及堆位分布；上层按堆位网格绘制在库货物与入库预约格子
- **平面图设置迁入系统设置**：移除堆位管理「平面图管理」弹窗；系统设置新增 Tab「平面图设置」，左右布局（左侧网格画板、右侧工具区），网格密度为行×列（如 26 行 × 42 列）；可绘制整个园区（五仓 + 3 仓停用 + 办公楼 + 道路/灌木图标重复铺贴）；仓库、堆位名称在左下角，门 / 设备间 / 功能区 / 生产线 / 办公楼名称居中；仓库为外层，内可绘制生产线、门、堆位
- **平面图网格密度改为行列**：系统设置 · 平面图设置的网格由像素改为行×列；吸附、道路/灌木铺贴与画板格子按行列计算；旧数据仅有 `grid` 像素时按画板尺寸换算行列
- **平面图编辑缩放与标注**：平面图设置画板支持滚轮缩放（1x–8x，可精细绘制）、空格/中键平移；名称按类型分区显示（仓库/堆位左下角，其余居中）
- **堆位内部网格**：堆位档案可设置列×行（如 10×10、10×3）；入库预约须选择预定堆位并拖选堆放格子，驾驶舱同步标出
- **入库预约网格交互**：预定堆放区改为表单右侧小格；单击切换选中，拖选用起点决定加选或取消（可反选）；打开弹窗不预选格子，需自行圈选
- **驾驶舱平面图放大与下钻**：仓库堆位示意图区域加大，仓储作业链路加高；平面图不再点击仓库跳转；点击堆位进入堆位管理，点击在库矿物格子跳转库存查询并定位该票
- **平面图仓库缩放与整图平移**：缩放仓库时内部堆位/门/生产线按比例同步缩放；放大整图后可拖拽平移画板（空白处、未选中图形、右键或空格）
- **平面图工具精简**：移除「恢复默认园区」以及左旋/右旋 90° 按钮

## 2026-09-07

### 已完成

- **驾驶舱作业链路时间**：仓储作业链路由「xx时xx分」改为「yyyy-mm-dd HH时」，精确到小时、不含分钟
- **驾驶舱入出库趋势图例**：近 7 日趋势统一湿重，图例由「入库湿吨 / 出库干吨」改为「入库湿吨 / 出库湿吨」
- **库存查询时间范围**：由单日改为「本月」或自定义起止（精确到时，示例 2026/8/01/8时–2026/8/2/16时）；入库时间列同步到小时；查询按该范围过滤列表
- **出库报关单金额与备注**：出库列表/新建单增加报关单金额（OCR 回填，可改币种）与备注；出库报关单 OCR 增加金额识别；全部 OCR 识别字段支持手工修正，修正写入操作日志（含修改前后值）
- **生产货物类型**：投料出库、加工过程、完工关联矿物及投料弹窗筛选，物料列改为展示**达标矿/报备矿**（与库存货物类型一致）

## 2026-09-06

### 已完成

- **物料档案字段收敛**：移除备案号、商品编码、品质参数；报备矿仅维护矿源。入库预约按矿源自动匹配当前有效且有剩余的备案号；品质参数记入上架后的本票库存（库存列表「本票品质」、重量台账）
- **本票单证详情**：入库已称重/已上架「详情」查看该票报关单、重量证书、品质证书及品质参数；库存查询同步增加「详情」（与「台账」并列）
- **堆位仓库下拉**：去掉初始化里残留的 `renderMatQualityFields` 调用，恢复仓库选项填充与切仓筛选
- **汇报第 13 页往来主体文案**：按原型三类角色补齐旁注、条目与讲稿备注
- **角色配置权限弹窗**：系统设置「配置权限」按菜单分组勾选可见模块，预勾该角色默认权限，保存后侧栏同步过滤

## 2026-09-05

### 已完成

- **方案汇报 PPT 改稿清单**：按当前原型整理 `docs/汇报/方案汇报PPT改稿.md`，逐页给出标题/正文/备注替换稿与截图对照（不改 pptx 文件，图片由人工截取）

## 2026-09-04

### 已完成

- **堆位平面图管理**：堆位页仅单仓库展示（默认第一仓）；工具栏增加「平面图管理」；弹窗按库容比例绘制堆位并标注仓号/堆位号，可输入门与功能区数量后拖拽缩放；保存后驾驶舱点击仓库模型展示，未设置则不弹出；展示时按在库重量划分堆位空间并标注物料
- **平面图功能区参与空间分配**：功能区（设备间、过道等）按数量预留库内面积，初始堆位在剩余空间按库容比例排布；已去掉中间红色过道线；此后堆位作为独立图形编辑，不再随功能区自动重排
- **平面图改名**：可点选门或功能区后在右侧修改名称（亦支持双击），保存后驾驶舱同步展示
- **4 仓堆位库容**：八个堆位改为大小不一（1.2 万～0.45 万吨），合计仍为 7.5 万吨，平面图按库容比例拉开宽度
- **平面图弹窗尺寸**：管理弹窗改为约占屏幕宽高各 60%，绘制区随弹窗撑满
- **平面图不拆堆位、限制网格内**：每个堆位保持整块；功能区/门拖拽与缩放不得超出红色网格范围
- **平面图堆位拖拽/旋转/矩形**：堆位与功能区仅矩形（拖拽、四角缩放、90° 旋转）；已去掉多边形顶点编辑；松手后边缘吸附并自动消除重叠
- **平面图交互（网格/撤销/选中）**：拖拽与缩放对齐 20px 网格，旋转按 90° 吸附（Shift 临时关闭）；单击选中虚线框 + 包围盒顶旋转点；支持撤销/重做（Ctrl+Z / Ctrl+Y）
- **开源平面图库调研**：warehouse_layout_editor / ai_room_planner / Planorama 均无中文 i18n；与本仓「堆位绑定 + 功能区多边形 + 驾驶舱重量叠加」模型不匹配，不建议接入，继续自研 SVG 编辑器
- **平面图功能区回写**：再次打开编辑器时按已存数量保留功能区坐标，不再因面积过小重置到右侧默认条
- **平面图后端存储**：保存/读取走 `GET/PUT /api/warehouses/{id}/floor-plan`（全量 `GET /api/floor-plans`）；旧 localStorage 仅一次性迁移
- **平面图标签排版**：名称随矩形宽高自动横排、换行或竖排并缩字号
- **平面图门宽**：门不再强制最小宽度 40，可缩成细条
- **平面图 PUT 报错**：保存接口回包改为短 JSON（不回传整图）；响应用 ASCII JSON + `no-transform`，避免入口 Nginx gzip 与 Content-Length 冲突导致浏览器 `ERR_CONTENT_LENGTH_MISMATCH`

## 2026-09-03

### 已完成

- **出库完成**：出库列表增加「出库完成」按钮；已审核单据可单次完成并自动更新出库/完成时间
- **出库委托方**：出库模块委托方统一为南国铜业（广西南国铜业有限责任公司）
- **出库 OCR**：新建出库单弹窗增加报关单上传区与必传标识；上传或扫描后 OCR 识别回填报关单号、核注清单号、流向企业与重量；提交前校验 OCR 字段

- **批次字段统一**：各模块「在库批次/批次号/批次类型」拆分为**报关单号**与**生产批次**两列（或对应单字段）；堆位、库存、移库、关务核销等已同步

- **入库 OCR 文案**：新建入库预约弹窗 OCR 按钮「扫描提单/报关单」改为「上传提单/报关单」；出库弹窗同步

- **入库品质参数**：收货登记列出铜/银/金/砷/铅/镉/氟/汞等品质项（铜为必填）；系统参数可新增/删减品质参数；入库收货与 OCR 品质证书识别字段随配置同步

- **物料档案品质参数**：列表「品质参数」列按系统配置将全部元素拼接为字符串展示（如 Cu≥22%；Ag≤50g/t；…）；编辑弹窗分项录入并实时拼接

- **入库金额币种**：入库列表金额支持人民币（¥）与美元（$）；新建预约可选择币种；OCR 识别回填币种与金额

- **入库列表排序**：四条演示数据按状态顺序展示——待收货 → 已称重 → 已上架

- **原料投料筛选**：投料出库弹窗原料列表支持按报关单号、物料、来源国家、堆放堆位筛选

- **操作日志查询**：增加报关单号、生产批次筛选条件；留痕行绑定对应标识便于追溯

- **库内移库**：「整托移位」改为「整票移位」、「拆批移位」改为「拆票移位」；批量移库支持勾选多个原料单（报关单号）同时移位

- **物料档案附件**：列表增加附件列与「查看附件」；编辑弹窗支持上传；JPG/PNG/PDF/TXT 在线预览，其它格式下载

- **入库委托方**：入库列表与预约弹窗增加南国铜业（广西南国铜业）；委托方下拉显示简称；新建预约默认选中南国铜业

- **保税账册**：移除有效期字段及账册到期预警、系统参数中的到期提醒

- **往来主体**：角色「货主」改为「物流账册主体」；列表与弹窗移除联系人字段

- **矿源备案扣减与预警**：报备矿入库按干重扣减备案剩余量（剩余 − 本次干重）；用量达系统参数阈值（默认 80%）触发预警；阈值可在系统参数配置

- **堆位编码自动生成**：堆位档案中编码由仓库编号与堆位名称自动拼接（如 1仓 + A1 → 1#A1），只读不可编辑；修改仓库或堆位名称时实时更新

- **驾驶舱仓库平面图**：点击仓库 3D 模型弹出平面图（`assets/store.png`）；支持按钮放大/缩小、左旋/右旋、重置，以及滚轮缩放与拖拽平移；支持按仓上传平面图附件（JPG/PNG/WEBP/PDF，本地持久化）、下载与恢复默认

- **批次号统一为两种**：原料/进口在库身份取**报关单号**（上架后即库存批次，不再另编 PC 号）；成品在库身份取**生产批次号**
- **生产逻辑**：同一生产批次号可勾选多个报关单号矿物投料；加工/完工谱系按 1:N 展示；完工入库后成品库存批次即该生产批次号
- 库存、堆位、移库、出库、关务、预警、驾驶舱演示数据与操作日志同步改为报关单号 / 生产批次号

- 本地预览原型批注改为请求线上接口 `http://wms.skd.wang/api/annotations`（已在 `wms.skd.wang` 时仍用同源相对路径）；本地与线上共用批注数据，无需再起本地批注服务

## 2026-09-01

### 已完成

- 全站 UI 整体放大 **125%**（`html { zoom: 1.25 }`），登录页、业务页、弹窗、驾驶舱同步生效

- 驾驶舱「仓储作业链路」可视高度固定为 **6 行**；下方**生产示意卡片**改为 `flex: 1` 占满侧栏剩余高度，视频/场景区随高度拉伸

- 驾驶舱 KPI 卡片移除底部说明小字（`ck-kpi-sub`），仅保留指标数值与进度条

- **入库 OCR 回填**：新建入库预约弹窗顶部增加 OCR 区（扫描提单/报关单），识别后自动回填船名、提单号、报关单号、核注清单号、金额、物料等；支持打开识别详情人工修正
- **收货 OCR 回填**：到货收货登记增加重量单、品质证书两个 OCR 入口，识别后回填湿重/干重/水分、Cu 品位/品质证书号；OCR 详情弹窗按场景显示字段并支持「回填并归档」
- 入库页移除独立「OCR 识别」Tab（OCR 已内嵌于预约/收货弹窗）；列表行「OCR」快捷入口保留

- 操作留痕统一在**操作日志**页：片区标识变更、加工过程登记、移库、入库等；支持快捷筛选与**时间段查询**（起止日期）
- 编码规则：1#=1仓，1#A1=1仓A1堆位；共 36 个堆位；驾驶舱与业务页同步更新

- 重新引入**堆位**概念：仓库拆分为堆位（如 4#A1）；**片区标识**为堆位动态区域属性，随批次查验状态变更（待检区域→混成品区域）
- 堆位页含查验完成弹窗；片区标识变更历史移至操作日志页；驾驶舱恢复堆位热力条并按片区标识着色
- 入库/移库/库存/生产等业务字段改为堆位 + 片区标识

- 原型物料模块改为通用**物料档案**表述（业务仍仅铜精矿，不在界面体现）；移除**矿源档案** Tab 与 `modalSource`
- 侧栏「物料/矿源」改为「物料档案」；演示数据名称改为原料物料-A/B、成品物料-A

- 移除**堆位**概念：仓储层级简化为 **仓库 → 片区** 两级；库容与占用在片区维度维护
- 原型「仓库库位」改为「仓库片区」；删除堆位档案 Tab、`modalLocation`、驾驶舱堆位热力条
- 入库/移库/库存/生产/盘点等业务字段统一为「片区」；API 草案移除 `/api/warehouses/locations`

- 仓储层级由四级（仓库→库区→片区→堆位）简化为 **仓库→片区** 两级，移除库区层（后序再移除堆位层）
- 原型「库区库位」改为「仓库库位」；Tab「库区与片区」改为「片区档案」；删除库区档案弹窗与表格列
- 堆位/盘点/驾驶舱等文案统一为「仓库 / 片区」表述；API 草案移除 `/api/warehouses/zones`

## 2026-08-31

### 已完成

- 驾驶舱「仓储作业链路」由固定步骤时间线改为作业日志滚动播放：展示「xx时xx分 + 入仓/出仓/生产/称重/上架/完工」流水，无缝循环滚动，悬停暂停
- 驾驶舱原料/成品库存 KPI 同时保留湿吨与干吨双指标展示
- 驾驶舱「生产示意图」改为**生产数据**卡片：批次号、总生产量、生产日期/预计完工、累计生产重量；点击可下钻生产页
- 生产数据区视频改用 `scene.mp4`，已去除左上角「AI生成」水印（ffmpeg delogo）
- 侧栏隐藏「系统架构」入口，禁止各角色访问（`HIDDEN_PAGES`）
- 原型批注侧栏支持导入 JSON：可与现有批注合并（同 id 更新）或整表替换
- 原型批注改为远程同步：新增 `wms/api` 服务 + Nginx `/api` 反代；多设备拉取/推送/轮询合并；侧栏显示同步状态；本地缓存兜底；首次可将本机旧批注迁移到服务器
- `api/Dockerfile` 基础镜像改为 DaoCloud 代理（解决国内 ECS 拉不到 `node:20-alpine`）；npm 使用 npmmirror
- 批注 API 改为宿主机 **Python 标准库** 常驻（`server.py` + systemd），Docker 仅跑 nginx；规避镜像源 TLS 超时
- 登录状态写入 `sessionStorage`（`wms_auth`）：同标签页刷新保持登录；退出清除；角色切换同步更新
- 批注完成后页面 Pin 自动隐藏；列表可筛「已完成」查看；点击列表项跳转并高亮左侧落点（光圈+标记）
- 驾驶舱底栏「预警与待办」改为仅「预警」，移除待办类条目
- 库区/片区关系调整为：**库区包含片区**；四级结构仓库→库区→片区→堆位；片区类型（原料/成品/待检）可调整并留痕；「库区轮换」Tab 改为「库区与片区」
- 盘点等业务文案由旧「原料区 A」统一为「库区 / 片区」表述；补全作业库区片区 M-02 示例
- 物料模块改为主做铜精矿：**铜精矿档案 + 矿源档案**双 Tab；矿源独立建档并被铜精矿引用

## 2026-08-26

### 已完成

- 顶栏角色标签改为鼠标移入下拉选择（管理员/仓管员/关务员/生产员），自动刷新菜单并在无权限时跳转默认页
- 登录页账号类型默认选中「系统管理员」
- 原型移除「铜精矿混矿」「五仓三区」相关副标题：驾驶舱顶栏副标题、成品 KPI 说明、作业链路「混矿完工」节点
- 原型系统品牌标题统一为「WMS系统」：浏览器标题、登录页、侧栏、驾驶舱顶栏、系统架构横幅
- 原型分角色登录控制侧栏菜单：`ROLE_PAGES` 按仓管员/关务员/生产员/管理员过滤可见项，空分组自动隐藏；`go()` 拦截越权跳转
- 驾驶舱库区仓库示意图改为 C4D 等距矢量 SVG 建模，去除背景图与热力闪烁动效
- 新增原型页「系统架构」：四层架构图（UI 设计 / 功能设计 / 后台管理 / 设备保障），功能标签可点击跳转对应模块；侧栏概览组入口
- 修复系统架构图左侧层级标签文字倒置（去掉 vertical-rl + rotate(180deg)，改为正向竖排）
- 系统架构图移除「移动端预留」卡片及「设备保障」整层
- 系统架构图改为技术分层：展示层 / 应用层 / 服务层 / 数据层
- 系统架构图移除后台管理三列区块（企业级/仓库级/堆位级）及服务层
- 原型批注支持右键切换完成状态（标记/批注列表/侧栏列表）；双击仍可编辑
- 批注侧栏增加状态筛选：全部 / 未完成 / 已完成
- 系统架构图扩展为五层（展示/应用/服务/数据/基础设施），应用层恢复九大业务模块细粒度标签
- 系统架构图去除数据库、操作系统等具体产品名称与版本号
- 系统架构图「信创运行环境」改为「国产运行环境」，副标题统一为国产表述

## 2026-08-25

### 已完成

- 制作汇报 PPT `docs/汇报/WMS保税仓仓储系统开发内容汇报.pptx`：按页面说明开发内容，**左文右图**（右侧原型截图）；截图 `docs/汇报/screenshots/`，脚本 `capture_wms_screenshots.mjs` + `generate_wms_plan_ppt.py`
- 依据 `docs/@原始需求.txt`、询比采购书附件一、`docs/投标/投标-正标.docx` 技术标，重写 `docs/项目设计.md` 与 `docs/开发计划.md`
  - 补充铜精矿混矿业务背景（加工后仍为铜精矿）、五仓库区及库区不定时轮换约束
  - 明确库存预警参数：库容 70%、原料库龄 90 天
  - 对外对接：海关 + 物流（原始需求必预留），园区/ERP/生产（采购扩展预留）
  - 对齐合同 55 自然日三阶段（30+10+15）、节点工期 D5–D55、五批功能交付与验收/付款节点
  - 开发计划由抽象 1–4 期改为与合同阶段、里程碑、并行专项（信创/OCR/等保/接口）一一对应
- 梳理并完善 `wms/dist/` 原型，对齐项目设计：
  - 演示数据由镍矿/镍铁统一改为铜精矿（原料/成品），货主改为广西丰联铜业
  - 库区库位新增「堆位档案 / 库区轮换」双 Tab 及轮换弹窗、历史留痕
  - 系统参数：库容预警 70%、原料库龄 90 天；预警页与驾驶舱指标同步
  - 对外对接：海关、物流标为必预留，新增联调日志示例；生产工序改为混矿筛分
  - 驾驶舱改为五仓卡片：每仓独立文生图背景 + 底部 20% 堆位热力条；点击堆位或仓名跳转库区库位页并带筛选
  - 同步更新 `docs/UI设计.md` §5.1
- **原型批注**：顶栏「显示/隐藏批注」与「批注列表」；**右键任意处**落点并弹出仅含输入框的编辑窗；数据持久化至 `localStorage`（`wms_proto_annotations`），显示状态记入 `wms_proto_anno_visible`

## 2026-08-16

### 已完成

- 修复 `wms/dist` 弹窗样式：大弹窗类名由 `lg` 改为 `modal--lg`，避免与驾驶舱图例 `.lg`（8×8 色点）冲突导致弹窗被压成 8px 白条；完善 modal 纵向 flex / 内部滚动与缓存版本号

## 2026-08-15

### 已完成

- 扩写 `docs/投标/投标-正标.docx` 整份技术方案的小节前文概述：补充技术方案总述、单位与经营能力、十大业务模块、实施试运行、本地部署、团队、服务响应、等保整改及运维培训等 **56 处**章节引导，强化建设目标、业务价值、实施边界、控制原则和验收衔接；生成 `投标-正标-概述扩写前备份.docx`，同步更新 152 页 PDF 预览
- 完善 `docs/投标/投标-正标.docx`：对采购文件十大模块全部细项逐项编写实现方案，替换原简略响应表，并嵌入 10 张模块原型图及 1 张智慧化驾驶舱图；保留 `投标-正标-修改前备份.docx`
- 将实施阶段、节点工期和功能分批交付三节由表格改为详细文字，工期统一为采购要求的 **30+10+15 个自然日、合计 55 个自然日**，补充阶段输入、活动、交付物、确认机制、里程碑和分批验收标准
- 扩充上线试运行计划，覆盖组织职责、环境数据、全链路场景、并行核对、问题分级、日报周报、性能安全、备份恢复、应急回退、切换准入和验收成果
- 制定分角色培训、知识转移与资料移交计划，明确场次、课程、实操、80 分考核/补训闭环、反向演示、资料清单及签收验收
- 补齐 `wms/dist/` 投标原型：五仓档案、批量移库、批次重量台账、库龄预警、关务差异穿透、ERP/海关端口和操作审计详情；截图保存于 `docs/投标/原型截图/`
- 更新 `项目设计.md`、`UI设计.md`、`开发计划.md`、`API文档.md` 和 `README.md`，并导出 `docs/投标/投标-正标-预览.pdf` 完成版式抽检
- 依据广西丰联铜业 WMS 询比采购要求及参考标书，重写 `docs/投标/正标文字` 的响应机制、系统故障与应急预案、智慧化与数据可视化、等保二级实施方案、运维管理方案；删减重复表述，补充服务时限、应急操作、数据核验、指标口径、等保交付及运维验收依据

## 2026-08-14

### 已完成

- 按正标成员表撰写团队简历 Excel：`docs/投标/团队.xlsx` / `团队.excel`（图示表头+主要工作经历；项目选自尾部近三年业绩 8 项；含 10 人分表）
- 右上角演示用户名由 demo 改为 admin
- 同步文档：`UI设计.md` §6 驾驶舱规范、`项目设计.md` 模块十一、`API文档.md` 驾驶舱汇总接口

## 2026-08-11

### 已完成

- 按询比采购书生成投标 Word：`docs/投标/广西丰联铜业WMS系统-投标文件-20260811.docx`（封面配图、全文宋体；标题24加粗／二级18加粗／三级16不加粗／正文14；含商务报价、分项报价、技术方案8节、商务方案、附件清单；建设含税总价￥466,000，限价内）
- 将上述投标文件全部表格改为纯文字描述（报价、功能响应、工期、硬件清单、团队、等保费用、年维、附件清单等），无表格，排版规范不变；输出 `docs/投标/广西丰联铜业WMS系统-投标文件-20260811-纯文字.docx`
- 工期阶段区分（正本仍为三阶段）：v1 改为两阶段（研制测评40日+部署验收15日）；v2 改为四阶段（15+15+10+15）；里程碑/分批计划/试运行/现场支持表述已同步，合计仍为55自然日
- v1/v2 去掉“常驻”承诺：改为不设现场常驻，仅上线关键节点（联调/培训/初终验）到场；章节改为“现场支持安排”；成员表、阶段三、应急与到场表述同步（正本未改）
- 通查并修正 v1/v2 团队与驻场一致性（不改正本）：组织架构、成员表、驻场专节、应急组织、服务到场、阶段三工作内容对齐；v1 常驻实施1人+运维远程，v2 常驻运维1人+实施节点到场；清除 v2「驻场≥2人」矛盾表述
- v1/v2 技术标减项精简（**不改正本**）：硬件按采购基线配置（8核/32GB/480GB/960GB、去掉万兆超配）；中间件不单列、网络设备按需利旧；团队压至5人；v1 可视化去掉穿透/预警，v2 保留穿透但不单建预警中心；备份 v1 单块2TB、v2 单块1TB；功能/计划/等保/运维表述同步压缩，硬性点（麒麟V10、达梦V8.4、55日、等保二级、十大功能）保留
- 深度拉开 v1/v2 与正本差异：重写功能响应/计划/硬件/服务/等保/附件等表头与单元格措辞，团队表列名与分工表述区分；正文段落与应急条款再改写；三份相同非空单元格由约90%降至约16%（硬性要求、麒麟+达梦、55日等仍保留）
- 调整 `docs/投标/投标文件20260811-v1.docx`、`投标文件20260811-v2.docx` 排版/格式/文字，与正本拉开差异：v1 为仿宋正文+黑体标题+副本封面；v2 为宋体正文+楷体章节（第×部分/（一）体例）+左对齐封面；章节标题与表述同步改写
- 陪标 A/B 操作系统、数据库按招标文件调整为**银河麒麟服务器操作系统 V10**、**达梦数据库管理系统 V8.4**（正本仍为统信+海量）
- 基于 `docs/投标/投标文件20260811.docx` 生成两份陪标（仅改文字、保留样式）：`投标文件20260811-陪标A.docx`、`投标文件20260811-陪标B.docx`；技术方案表述与团队名单各不相同；已去掉评分提示、粘贴占位、模板完结等内部提示语
- 修改 `docs/投标/投标文件20260811.docx` 信创选型：操作系统由银河麒麟 V10 改为**统信服务器操作系统 UOS Server**；数据库由达梦 V8.4 改为**海量数据库 Vastbase**（架构说明、公告截图标题、软硬件清单、实施阶段表述已同步；备份 `投标文件20260811.bak.docx`）
- 统一 `docs/投标/投标文件20260810 copy.docx` 技术方案正文编号：去掉「一、二、三」「（1）（2）」混用，改为 `3.x.x.x` 或连贯表述
- 更新 `docs/投标/投标文件20260810 copy.docx` §3.4.2 专项实施小组：配置 9 人（含 5 名研发），删除联系电话列；项目经理/测试采用年长风格姓名

## 2026-08-10

### 已完成

- 填充 `docs/投标/投标文件20260810 - 副本.docx` 技术方案（附件四 8 节）：单位简介、系统建设方案（功能响应+55日计划）、本地服务器清单与基线自检、团队驻场、响应/应急、可视化、等保方案、运维；商务报价/分项报价/商务方案等其它章节未改
- 修正 `docs/投标/投标文件20260810.docx` 报价表版式：商务报价表、分项报价表按甲方附件二/三原表结构（7列左右对照、8列含备注）对齐复制，填写区留空
- 调整 `docs/报价/WMS项目报价单_付款更新.xlsx` 首页：删除底部说明与税差测算表；在「固定费用合计」后增加简短税额说明（按 6% 计列，开 1% 票须补足税差）
- 按甲方询比采购书重写投标响应模板：`docs/投标/投标文件20260810.docx`
  - 对齐「三、供应商提交的采购响应文件」四册结构：附件二商务报价表、附件三分项报价表、附件四技术方案（8节）、附件五商务方案（3节）
  - 覆盖评分/硬性要求占位：功能响应对照、55日分期计划、服务器软硬件清单与基线自检、等保方案及费用、运维/应急、资质业绩、质保与年维及二次开发工时
  - 签名、日期、金额、周期、人员等关键字段全部留空；生成脚本 `_gen_bid_template.py` 可复跑
- 输出投标（询比响应）文件框架：`docs/投标文件框架.md`
  - 依据采购文件四册结构（附件二～五）+ 综合评审分值映射
  - **技术第 2 章按评分 20 分细则强制展开**：功能逐项核验（附件一第三点十模块）+ 实施阶段划分 + 分阶段工作内容 + 节点工期计划表 + 功能分批落地 + 上线试运行计划
  - 同步服务器 40 分/*硬性、服务响应、可视化、团队、运维、等保、商务 25/30/45 及冲分自检
- 按商务反馈调整报价单（税率与税差）
  - `docs/WMS项目报价单.xlsx`：税率由 1% 改为 **6%**；补充说明——若实际只能开 3%/1%，税差补给苏总；新增税差测算表（3%→¥5,094.34；1%→¥8,490.57）
  - `docs/报价单.md`：同步含税 6%、税差约定及客户版摘要
  - 固定开发费仍为 ¥180,000（含税）
- 付款条件对齐采购文件：由 30%/30%/30%/10% 改为 **预付款 30% + 验收款 65% + 质保尾款 5%**
  - Excel「付款方式」与 `报价单.md` 第四节已同步（含开票、7 个工作日、等保 2 级备案等条件表述）

## 2026-08-09

### 已完成

- OCR 红框识别调研：依据 `docs/ocr识别示例/` 三张标注样例编写可用脚本
  - 目录：`docs/调研/`（`ocr_red_boxes.py`、`requirements.txt`、`README.md`）
  - 能力：OpenCV 检测红框（滤除公章）→ RapidOCR 识别 → 按单据类型结构化字段
  - 已跑通：报关单关键字段、重量证净湿重/干重/水分、检验证项目（Cu/Ag/Au…）；样例品质结果列被打码故为 null
  - 结果示例：`docs/调研/ocr_red_boxes_result.json`
- OCR 可视化工具：`docs/调研/ocr_app.py`（Gradio，左上传/预览、右结构化结果）
- OCR 单据模式：报关单 / 重量证书 / 检验证书（限制红框数 + 降多尺度提速；CLI `--mode` / UI 单选）
- OCR 失败不回退其它模式；结果增加识别时长 `elapsed_ms` / `elapsed_sec`
- OCR 流程改为：先校验图片是否匹配所选单据模式，不匹配返回「无效图片，请重新上传」，匹配后再红框 OCR
- 落档信创（统信 UOS Server + 海量 Vastbase）与等保对开发的阻碍及规避原则
  - `项目设计.md`：新增 §7（硬约束、阻碍清单 A–E、规避原则、风险缓解）
  - `开发计划.md`：1 期纳入信创冒烟与审计基线；3 期 OCR 信创选型；4 期等保材料与整改
  - `部署指南.md`：正式环境改为统信+海量；裸机 systemd 为主、容器可选
  - `README.md`：补充正式环境约束入口说明
- 后端技术栈由 Node 调整为 **Java（Spring Boot）**，同步更新项目设计、开发计划、部署指南、API 文档、README

## 2026-08-06

### 已完成

- 编制项目报价单：`docs/报价单.md`（含开发费、出差费预估、首年运维费及付款节点）
- 输出 Excel 版报价单：`docs/WMS项目报价单.xlsx`（客户版：开发 18 万；外地差旅实报实销；首年运维优惠 0 元）

## 2026-08-03

### 已完成

- 阅读需求文档《新建 文本文档.txt》，梳理 WMS 十大功能模块
- 初始化文档：`项目设计.md`、`UI设计.md`、`开发计划.md`、`API文档.md`、`部署指南.md`、`README.md`
- 制作可交互 HTML 原型：`prototype/index.html`（含样式与脚本）
  - 登录（多角色含海关专用账号）
  - 工作台及侧栏导航
  - 基础数据 / 入库 / 移库 / 库存盘点预警 / 生产完工 / 出库 / 关务 / OCR / 对接 / 审计日志
  - 关键业务弹窗表单（新建、收货、OCR 回填、差异审批等）
- 补充部署说明：一台 Nginx 多站点配置（双 conf）方式，写入 `部署指南.md`
- Docker 部署：域名定为 `wms.skd.wang`，更新 `depoly/nginx.conf`、`depoly/docker-compose.yml`、`部署指南.md`
- 处理 80 端口冲突：compose 改为映射 `8081:80`，并补充反代说明
- 修复 `wms.skd.wang` 被易手办抢占：去掉 `server_name` 中的 `_`，新增 WMS 反代块；compose 增加 `host.docker.internal`

### 待办

- 将更新后的易手办 `deploy/nginx.conf` 与 `docker-compose.prod.yml` 同步到服务器并重载 nginx
- 确认 `~/wms` 容器在 8081 运行后访问 `http://wms.skd.wang`
- 原型走查后按反馈微调页面与字段
- 如需正式 `.rp` Axure 工程文件，按页面清单在 Axure 中还原或另行导出
